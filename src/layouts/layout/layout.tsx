import React, { FC } from 'react'
import styled from '@emotion/styled'

interface LayoutProps {}

const _Layout: FC<LayoutProps> = () => {
  return (
    <Layout>
      
    </Layout>
  )
}

const Layout = styled.div``

export { _Layout as Layout }
